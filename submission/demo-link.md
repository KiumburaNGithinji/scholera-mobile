# Demo Video

**Link:** _paste your video URL here before submitting_

Recommended hosts (any one):
- Loom (fastest)
- YouTube (unlisted)
- Google Drive (set link to "Anyone with link can view")

## What the video covers

5–10 minutes:
1. Sign in as admin → tour the home + steel-accented tab bar → sign out
2. Sign in as professor → tour + clay accent → sign out
3. Sign in as student → tour + sage accent → sign out
4. Walk through `/dev/preview` showing the 7 primitives under all 3 role themes
5. Brief code tour: `providers/auth-provider.tsx` (two-step pattern), `app/_layout.tsx` `ProtectedRouter` (splash gate), `theme/role-theme.ts` (single CSS var swap)
6. Honest closing: what would ship next (Phase 4/5/6) and why I shipped the foundation rather than a half-broken full demo
